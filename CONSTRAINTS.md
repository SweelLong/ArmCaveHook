# ArmCaveHook 插件编程约束

本文档是本项目面向 LLM 和自动化编码代理的项目级约束。处理插件开发、修改、调试和排障任务时，必须优先遵守本文档；如果用户给出更具体且不冲突的要求，以用户要求为准。

## 总体原则

- 先理解框架的设计和现有 API，再设计实现。优先复用框架提供的能力，不要为了短期方便重新实现框架已有的逻辑。
- 能内联的逻辑尽量内联，避免不必要的辅助函数、全局状态和额外抽象。只有在确实改善复用性、可读性或框架绑定关系时，才增加函数。
- 每个结论都应有代码、工具输出、运行结果或可复现现象作为依据。区分“已证实”“高概率推断”和“尚未验证”，不得在证据不足时直接下结论。
- 修改应保持最小范围，避免无关重构、格式化和元数据变更。

## Git 使用限制

- 不要把查看 Git 状态、差异、历史或提交记录作为默认动作，也不要连续、重复地查看 Git。
- 只有在已经有明确理由、且 Git 信息确实能回答当前问题时，才可以查看相关 Git 信息。每次查看前应先明确要验证的具体问题。
- 不要用 Git 检查来替代对源码、构建产物、运行日志和实际行为的分析。除非证据指向版本或补丁应用问题，否则不要反复怀疑“补丁文件没有被正确使用”。
- 不得为了排查普通代码问题而执行会覆盖、回退或清理用户改动的 Git 操作。

## 汇编与框架 API

- 禁止使用 `asm volatile()`，也不要在插件中手写内联汇编或用手写汇编替代框架能力。
- 需要创建汇编函数、注入代码或完成相关底层操作时，优先使用框架原生 API，例如名称式的 `new_asm_func`、`new_cpp_func` 和 `patch_asm`。先查阅项目头文件和文档中的 API 定义与使用方式。
- 非必要不要新增 `include` 或引入额外库。优先直接使用 `bind` 绑定框架内部已有符号、内部函数或已知函数偏移，复用目标程序和框架已经提供的能力。
- 如果确实必须新增头文件或库依赖，应先确认没有可用的内部绑定方案，并说明新增依赖解决的具体问题、必要性以及对插件体积和兼容性的影响。
- 只有在确认框架没有对应能力，并且用户明确要求或确有不可替代的必要性时，才讨论其他底层方案；不能默认回退到手写汇编。

## 插件结构与命名约束

- `patch_` 系列的补丁调用和注册逻辑原则上集中写在 `init()` 中，能直接写在 `init()` 的就不要额外拆出函数。
- 需要额外函数或外部绑定时，优先使用框架提供的 `hook_`、`new_` 等绑定 API。所有 `patch_`、`new_`、`hook_` 系列 API 的调用、注册和绑定都必须在 `init()` 内完成。
- 注意：`init()` 外声明的变量以及 `static` 变量可能被写入插件数据段。除非确有必要，不要引入这类状态；优先使用局部变量、初始化流程内的状态或框架提供的数据管理方式。
- 不要仅为了形式上的模块化，把本来可以内联的短逻辑拆成多个外部函数。外部函数必须有清晰的生命周期、绑定关系和存在理由。

## 排障与逆向分析顺序

按以下顺序选择工具，避免无依据地扩大排查范围：

1. 优先确认环境中是否有可用的 `ida-pro-mcp`，并使用它查看函数、交叉引用、调用关系和反编译逻辑。
2. 如果没有 `ida-pro-mcp` 或其信息不足，再使用 `otool`、`objdump` 等二进制分析工具直接查看指令、符号、重定位和调用逻辑。
3. 只有前述工具无法完成任务时，才考虑 Python 相关库，例如 Keystone 等；先确认库是否已安装以及它能解决哪个具体问题。

排障时应先验证最接近现象的代码路径和框架 API 用法，再逐步扩大范围。不要因为问题涉及补丁，就默认补丁文件加载、应用或使用方式是根因；只有日志、构建结果或运行行为提供了证据时，才检查该方向。

## 日志与验证

- 在用户测试成功之前，日志必须足够详细，能够记录关键路径、参数、地址、函数绑定、补丁应用结果、错误分支和必要的返回值。日志内容应服务于定位问题，而不是只打印“成功/失败”。
- 调试阶段应保留能证明因果关系的上下文，并通过最小可复现步骤逐项验证假设。
- 用户确认测试成功后，再删除或清理临时调试日志，保留项目长期需要的必要错误日志和诊断信息。
- 在报告结果时说明实际验证过的内容、尚未验证的假设以及可能的剩余风险；不能把“编译通过”表述成“运行逻辑已正确”。

## 实施前检查清单

实现或修改插件前，至少确认：

- 是否已有可直接复用的框架 API；
- 是否可以通过 `bind`、内部符号、内部函数或函数偏移完成需求，而无需新增 `include` 或外部库；
- 是否误用了内联汇编或 `asm volatile()`；
- `patch_`、`new_`、`hook_` 的调用和绑定是否都在 `init()` 内；
- 是否引入了不必要的 `init()` 外变量或 `static` 变量；
- 是否真的有证据支持当前排障假设；
- 当前日志是否足以让用户完成测试并反馈可定位的信息。
